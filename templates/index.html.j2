<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>My RSS Reader</title>
    <style>
        body {
            font-family: system-ui, sans-serif;
            background-color: #f9f9f9;
            color: #333;
            margin: 2rem auto;
            max-width: 800px;
        }
        h1 {
            text-align: center;
        }
        .entry {
            background: white;
            border-radius: 10px;
            padding: 1rem 1.5rem;
            margin-bottom: 1rem;
            box-shadow: 0 2px 6px rgba(0,0,0,0.1);
        }
        .entry h2 {
            margin: 0;
            font-size: 1.2rem;
        }
        .entry a {
            text-decoration: none;
            color: #007acc;
        }
        .entry a:hover {
            text-decoration: underline;
        }
        .feed-name {
            font-size: 0.9rem;
            color: #666;
        }
        .published {
            font-size: 0.8rem;
            color: #999;
        }
    </style>
</head>
<body>
    <h1>🗞️ My RSS Feed Reader</h1>
    {% for entry in entries %}
        <div class="entry">
            <h2><a href="{{ entry.link }}" target="_blank">{{ entry.title }}</a></h2>
            <div class="feed-name">{{ entry.feed }}</div>
        <div class="published">
            {% if entry.display_date %}
                {{ entry.display_date.strftime('%Y-%m-%d %H:%M') }}
            {% else %}
                Unknown date
            {% endif %}
        </div>
        </div>
    {% endfor %}
</body>
</html>
